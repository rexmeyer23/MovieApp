using MovieApp.Services;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;

namespace MovieApp.Controllers
{
    public class AdminRemoveController : ApiController
    {



        //public IHttpActionResult Delete (int id)
        //{
        //    var service = CreateAdminService();
        //    if (!service.RemoveProfile(id))
        //        return InternalServerError();
        //    return Ok();
        //}

        //private AdminService CreateAdminService()
        //{
        //    var profileID = Guid.Parse(User.Identity.GetProfileId());
        //    var adminService = new AdminService(profileID);
        //    return adminService;
        //}
    }
}
